# Configuration for circle packing constructor evolution (n=26)
# Ensemble via the school's Gateway API: Gemini 3.7 Flash (primary, 80%) + Gemini 3.1 Pro (secondary, 20%)
max_iterations: 100
checkpoint_interval: 10
log_level: "INFO"
# Disabled: the school gateway forwards this straight to Google's Gemini backend,
# which rejects the "seed" field with a 400 error.
random_seed: null

# LLM configuration
llm:
  primary_model: "gemini-3.7-flash"
  primary_model_weight: 0.8
  secondary_model: "gemini-3.1-pro-preview"
  secondary_model_weight: 0.2
  api_base: "https://factchat-cloud.mindlogic.ai/v1/gateway"
  temperature: 0.7
  top_p: 0.95
  # gemini-3.1-pro-preview is a reasoning model; its internal thinking tokens count
  # against max_tokens, so keep this at 16000+ or responses can come back empty.
  max_tokens: 16000
  # The school gateway sits behind Cloudflare with a hard 120s proxy timeout,
  # independent of this client-side timeout. Capping thinking effort keeps
  # generation well under that window instead of relying on max_tokens alone.
  reasoning_effort: "low"
  timeout: 110

# Prompt configuration
prompt:
  system_message: |
    You are an expert mathematician specializing in circle packing problems and computational geometry. Your task is to improve a constructor function that directly produces a specific arrangement of 26 circles in a unit square, maximizing the sum of their radii. The AlphaEvolve paper achieved a sum of 2.635 for n=26.

    Key geometric insights:
    - Circle packings often follow hexagonal patterns in the densest regions
    - Maximum density for infinite circle packing is pi/(2*sqrt(3)) ≈ 0.9069
    - Edge effects make square container packing harder than infinite packing
    - Circles can be placed in layers or shells when confined to a square
    - Similar radius circles often form regular patterns, while varied radii allow better space utilization
    - Perfect symmetry may not yield the optimal packing due to edge effects

    Focus on designing an explicit constructor that places each circle in a specific position, rather than an iterative search algorithm.
  num_top_programs: 3
  use_template_stochasticity: true

# Database configuration
database:
  population_size: 60
  archive_size: 25
  num_islands: 4
  elite_selection_ratio: 0.3
  exploitation_ratio: 0.7

# Evaluator configuration
evaluator:
  timeout: 60
  cascade_evaluation: true
  cascade_thresholds: [0.5, 0.75]
  parallel_evaluations: 4
  use_llm_feedback: false

# Evolution settings
diff_based_evolution: false
allow_full_rewrites: true
